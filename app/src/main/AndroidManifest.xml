<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stupidpeople.rhplus">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_rhplauncher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <!--android:theme="@style/AppTheme"-->
        <activity android:name=".DocsActivity">
        </activity>

        <activity android:name=".VideoListDemoActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".RadarChartActivitry" />

        <activity android:name=".DrawerActivityDocs" />
        <activity android:name=".DrawerActivityVideos" />
        <activity android:name=".DrawerActivityCale" />

        <activity
            android:name=".DrawerActivityRadar"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>
